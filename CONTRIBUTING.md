# Contributing

Run `mise run check` before proposing changes. Tests must be deterministic and
must not require Kinko, network access, a Meta account, or a billable asset.
Do not stage, commit, push, publish, deploy, or perform a live mutation as part
of this workflow.
